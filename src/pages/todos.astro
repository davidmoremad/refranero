---
import Layout from "../layouts/Layout.astro";
import Quote from "../components/Quote.astro";

const res = await fetch("http://localhost:4321/api/todos");
const refranes = await res.json();
console.log(refranes);
---

<Layout title="Refranero Popular Español" />
{refranes.map((refran) => <Quote>{refran.refran}</Quote>)}
