---
import Layout from "../layouts/Layout.astro";
import BigQuote from "../components/BigQuote.astro";

const res = await fetch("http://localhost:4321/api/aleatorio");
const refran = await res.json().then((data) => data["refran"]);
---

<Layout title="Refranero Popular Español" />
<BigQuote>{refran}</BigQuote>
