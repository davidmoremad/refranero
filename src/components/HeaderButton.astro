---
const { href, text, classes } = Astro.props;
---

<a
    href={href}
    class={`bg-stone-300 text-stone-800 rounded px-5 p-2.5 hover:scale-110 flex-grow ${classes}`}
>
    <slot>{text}</slot>
</a>
